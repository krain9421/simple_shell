#include "main.h"
#include <stdio.h>

/**
* getuserinput - stores user input in a string
* @buf: address of the string to hold the user input
*
* Return: number of characters stored
* 0 if nothing other than ENTER was typed
*/

int getuserinput(char **buf)
{
	size_t sz = BUFFSIZE;
	int bufsz;
}
